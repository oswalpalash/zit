name: CI

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  pull_request:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        zig-version: ['0.16.0']
    runs-on: ${{matrix.os}}
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: ${{matrix.zig-version}}
      
      - name: Run quality gate
        run: zig build quality

      - name: Check public build steps
        run: python3 scripts/check_build_steps.py --skip quality

  cross-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: '0.16.0'

      - name: Compile Linux smoke targets
        run: zig build smoke -Dtarget=x86_64-linux

      - name: Compile Windows smoke targets
        run: zig build smoke -Dtarget=x86_64-windows

  visual-repeat:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: '0.16.0'

      - name: Check deterministic visual targets
        run: python3 scripts/visual_repeat_check.py --count 4

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: '0.16.0'
      
      - name: Check formatting
        run: zig fmt --check src/ examples/ build.zig
      
      - name: Check scripts compile
        run: python3 -m py_compile scripts/check_build_steps.py scripts/visual_repeat_check.py

  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: '0.16.0'
      
      - name: Generate docs
        run: zig build-lib src/main.zig -femit-docs -fno-emit-bin
      
      - name: Check README exists
        run: test -f README.md

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build, cross-smoke, visual-repeat, lint, docs]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
